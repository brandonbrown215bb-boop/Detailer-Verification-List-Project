import React from 'react';
import ReactDOM from 'react-dom/client';
import { RuleEditorApp } from './RuleEditorApp';
import '../index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RuleEditorApp />
  </React.StrictMode>
);
